int main() {
    f0("0", "1", "2", "3", "4", "5", "6", "7");
    return 0;
}

void f0(string a0, string a1, string a2, string a3, string a4, string a5, string a6, string a7) {
    printString(a0 + a1 + a2 + a3 + a4 + a5 + a6 + a7);
    f1(a1, a0, a3, a2, a5, a4, a7, a6);
}

void f1(string a0, string a1, string a2, string a3, string a4, string a5, string a6, string a7) {
    printString(a0 + a1 + a2 + a3 + a4 + a5 + a6 + a7);
    f2(a3, a2, a1, a0, a7, a6, a5, a4);
}

void f2(string a0, string a1, string a2, string a3, string a4, string a5, string a6, string a7) {
    printString(a0 + a1 + a2 + a3 + a4 + a5 + a6 + a7);
    f3(a7, a6, a5, a4, a3, a2, a1, a0);
}

void f3(string a0, string a1, string a2, string a3, string a4, string a5, string a6, string a7) {
    printString(a0 + a1 + a2 + a3 + a4 + a5 + a6 + a7);
}

